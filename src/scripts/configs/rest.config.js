define(function () {
    'use strict';

    var rest = {
        users: '/users',
        fields: '/fields',
        clusters: '/clusters',
        boreholes: '/boreholes',
        psensors: '/psensors',
        tsensors: '/tsensors',
        usergroups: '/usergroups'
    };

    return rest;
});
